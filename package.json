{
  "name": "zotero-assistant-mcp",
  "version": "0.3.0",
  "private": true,
  "description": "Zotero MCP server for Cloudflare Workers (mcp-deploy compatible)",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist && mv dist/index.js dist/worker.mjs",
    "release": "npm run build && mkdir -p release-assets && cp dist/worker.mjs release-assets/worker.mjs && cp mcp-deploy.json release-assets/mcp-deploy.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.25.2",
    "agents": "^0.3.6",
    "zod": "^3.24.0",
    "zotero-api-client": "^0.45.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.0.0"
  }
}
